from turtle import *
color("black")

while True:
        forward(100)
        right(50)
        backward(75)
        speed(0)
           
       

        if abs(pos()) < 1:
            break


done()
